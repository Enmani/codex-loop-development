# Codex 循环开发系统安装说明

## 包内文件

```text
Codex循环开发系统-v1.1.0/
├─ loop-development/                 # 核心 Codex skill
├─ README.md                         # 项目简介与快速开始
├─ Codex循环开发系统使用教程.md       # 从零开始的中文教程
├─ tests/                             # 故障转移冒烟测试
├─ package.json                       # npm check/test 入口，无第三方依赖
├─ install.ps1                       # Windows 安装脚本
├─ 安装说明.md
└─ VERSION.txt
```

这个分享包不包含发送者电脑上的线程、自动任务、仓库路径或运行状态。安装后，使用者在自己的仓库里说“开始循环开发”，系统才会创建属于该项目的包工头、worker、独立监管任务和自动任务。运行中若总包的消息入口首次明确失败，系统先执行 archive → unarchive 原位修复；修复后重发仍失败，才通过持久状态创建继任总包。

## 系统要求

- Codex 桌面端。
- Node.js 可从终端执行。
- Codex 支持任务创建、任务读取、任务间消息和自动任务。
- 可以使用 Luna 模型；施工线程固定使用 `max` 推理，监管线程固定推荐 `xhigh`。

## Windows 自动安装

1. 解压 ZIP。
2. 打开 PowerShell，进入解压后的目录。
3. 建议先阅读 `install.ps1`。
4. 执行：

```powershell
Set-ExecutionPolicy -Scope Process Bypass
.\install.ps1
```

只验证压缩包内容、不安装：

```powershell
.\install.ps1 -ValidateOnly
```

开发者还可以运行完整冒烟测试：

```powershell
npm run check
npm test
```

安装脚本会自动选择：

- 如果电脑存在共享 agent 布局和 `sync-agents.mjs`，安装到 `~/.agents/skills/loop-development` 并同步。
- 否则安装到 `$CODEX_HOME/skills/loop-development`，默认即 `~/.codex/skills/loop-development`。

脚本不会覆盖已有同名技能。如果目标已经存在，它会停止并要求使用者先自行备份或处理旧版本。

如需安装到指定技能根目录：

```powershell
.\install.ps1 -InstallRoot "D:\my-codex-skills"
```

## 手动安装

不使用脚本时，将整个 `loop-development` 文件夹复制到：

```text
%CODEX_HOME%\skills\loop-development
```

没有设置 `CODEX_HOME` 时，默认位置是：

```text
%USERPROFILE%\.codex\skills\loop-development
```

macOS 或 Linux 可复制到：

```text
${CODEX_HOME:-$HOME/.codex}/skills/loop-development
```

## 安装后

1. 重启 Codex，或至少打开一个新任务。
2. 打开一个保存好的本地仓库项目。
3. 按 `Codex循环开发系统使用教程.md` 的“启动预检”执行检查。
4. 写好计划后，使用教程末尾的完整启动 Prompt。

最小验证 Prompt：

```text
请只检查能否加载 $loop-development，不创建任务、不修改文件、不创建自动任务。成功只回复 LOOP_DEVELOPMENT_READY；失败则说明缺少什么。
```

## 升级与卸载

升级前先备份已有 `loop-development` 文件夹，再用新版本替换。安装脚本为了保护用户文件，不会自动覆盖旧版本。

卸载时删除安装后的 `loop-development` 技能文件夹，然后重启 Codex。运行过程中生成的任务、自动任务和 durable state 是独立数据，不会因删除技能自动消失；应先在包工头任务中中止对应 run 并停用自动任务。
