interface:
  display_name: "循环开发协调器"
  short_description: "以线程消息驱动包工头、执行任务与兜底监理的循环开发"
  default_prompt: "Use $loop-development to start repository loop development from the active implementation plan."
policy:
  allow_implicit_invocation: true
